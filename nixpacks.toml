[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["pnpm --filter @stacks-playroom/api build"]

[start]
cmd = "node apps/api/dist/server.js"
