{
  "name": "@stacks-playroom/contracts",
  "version": "0.0.0",
  "description": "Clarity smart contracts for Stacks Playroom on-chain games",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "chokidar \"tests/**/*.ts\" \"contracts/**/*.clar\" -c \"pnpm test\"",
    "clean": "rm -rf .cache"
  },
  "dependencies": {
    "@stacks/clarinet-sdk": "^3.9.0",
    "@stacks/transactions": "^7.2.0",
    "@types/node": "^24.4.0",
    "chokidar-cli": "^3.0.0",
    "vitest": "^4.0.7",
    "vitest-environment-clarinet": "^3.0.0"
  }
}
